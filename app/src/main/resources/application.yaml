spring:
  application:
    name: microkernel-spring
  datasource:
    url: jdbc:postgresql://localhost:5432/microkernel_spring
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  mail:
    host: smtp.gmail.com
    port: 587
    username: hricaurtem@gmail.com
    password: aajf qvdj oocd iywk

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
